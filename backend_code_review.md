# Backend Code Review

리뷰 대상: `backend/` 전체 (main, routers, services, models, utils)
최초 리뷰: 2026-04-26
**해결 상태 갱신: 2026-07-27**

기준: 동작 정확성 → 안정성 → 설계/유지보수성 → 성능 순.

---

## 0. 갱신 요약

최초 리뷰 이후 대대적인 정리가 있었습니다. **P0·P1 항목은 전부 해결**되었고, P2도 대부분 반영됐습니다. 다만 그 과정에서 최초 리뷰가 **놓쳤던 더 큰 문제**가 드러났습니다.

| 등급 | 항목 수 | 해결 | 무효화 | 미해결 |
|---|---|---|---|---|
| Critical | 3 | 1 | 2 | 0 |
| High | 5 | 4 | 0 | 1 |
| Medium | 8 | 6 | 1 | 1 |
| Low | 7 | 2 | 1 | 4 |

"무효화"는 해당 코드가 삭제되어 문제 자체가 사라진 경우입니다.

### 최초 리뷰가 놓친 것

**(가) 스코어러가 한국어·일본어 곡을 구조적으로 전부 버리고 있었습니다.** 리뷰는 `calc_score`의 중복(4.1)과 매직 넘버(4.5)를 지적했지만, **이 함수가 주요 사용 사례에서 아예 작동하지 않는다**는 사실은 놓쳤습니다. `itunes.py`가 `country` 파라미터를 안 보내 US 스토어프론트로 가고, 거기서 온 영어 번역 제목을 한국어 입력과 비교해 유사도 0을 얻고 있었습니다. 41곡 벤치마크에서 한국곡 0/10, 일본곡 0/7이었습니다. 자세한 내용은 `README_BACKEND.md` 6장.

**(나) 커밋되지 않은 파일 때문에 배포 트리가 기동 불가였습니다.** `core/`, `utils/`, `services/url_resolver.py`, `pyproject.toml`, `uv.lock`, 모든 `__init__.py`가 git에 없어 `ModuleNotFoundError: No module named 'core'`로 죽었습니다. 리뷰는 작업 트리만 보고 git 상태를 확인하지 않았습니다.

**(다) 벅스 `lookup_by_id`가 깨져 있습니다.** 셀렉터 4개가 전부 안 맞아 모든 id에서 `None`을 반환합니다. 여전히 미해결(6장).

교훈: 코드 리뷰는 **실행해보고 git 상태를 확인해야** 합니다. 정적 읽기만으로는 이 세 가지를 다 놓칩니다.

---

## 1. 원래의 한 줄 요약

> 전체 구조(라우터·서비스 분리, Pydantic 스키마, 외부 API 추상화)는 깔끔하고 합리적임. 다만 실제로 터질 버그 3건이 핵심 경로에 있고, sync/async 혼용과 공용 코드 중복이 누적되고 있어 손볼 가치가 큼.

구조에 대한 평가는 지금도 유효합니다.

---

## 2. Critical

### 2.1 `parse.py` — `en_title`/`en_artist` 미정의 가능성 — ✅ **무효화**

MusicBrainz가 빈 후보를 주면 `UnboundLocalError`로 `/parse`가 500이 되는 문제였습니다. 먼저 `_resolve_english_query`에 폴백을 추가해 해결했고, 이후 **영문 질의 경로 자체를 삭제**하면서 문제가 사라졌습니다.

### 2.2 `musicbrainz.py` — `return []`이 루프 안에 있음 — ✅ **무효화**

`services/musicbrainz.py`가 삭제되었습니다. 이유는 아래.

> **MusicBrainz 제거 근거 (측정치)**
> - **품질**: 비ASCII 입력 5건 중 정답 2 / 빈결과 2 / **오답 1**. `물론`/허각 → `SunFlower (inst)` — 다른 곡이자 반주곡인데 `parse.py`가 `candidates[0]`을 그대로 사용.
> - **역효과**: 번역된 질의어가 멜론·벅스·FLO에도 전달되어, 맞은 경우조차 한국 플랫폼 매칭을 떨어뜨림. `밤편지`/`아이유`는 sim 1.0 정답에서 sim 0.54 오답으로 바뀌면서도 임계값을 통과했음.
> - **지연**: 0.83초가 최선, 통상 3~6초. `gather` 앞에서 직렬로 대기.
> - **동시성**: `musicbrainzngs`의 프로세스 전역 `threading.Lock`(라이브러리 383행) + `time.sleep`이 `_mb_request`(607행)를 감싸고 있어 `asyncio.to_thread`로도 못 빠져나감. 백엔드 전체가 초당 1회로 묶임. 비ASCII 8건 동시 요청 시 **15.31초**.
> - **대체**: iTunes `country` 파라미터 하나. 같은 조건에서 6/6 정답.

### 2.3 `parse.py` — 도달 불가 코드 + 잘못된 들여쓰기 — ✅ **해결**

해당 함수가 `_LOOKUP_BY_PLATFORM` 딕셔너리 분기로 재작성되면서 사라졌습니다.

---

## 3. High

### 3.1 URL 미인코딩 (Query Injection 가능성) — ✅ **해결**

`melon.py`, `bugs.py` 모두 `params={"q": title}` 위임으로 변경됐습니다. 곡 제목에 `&`, `#`, `?`가 들어와도 안전합니다.

### 3.2 sync I/O가 async 핸들러를 막음 — ✅ **해결**

전 서비스가 `httpx.AsyncClient`로 이관됐습니다. `import requests` 0건. `match.py`도 `await melon.search(...)`로 통일됐습니다.

> `pyproject.toml`에 `requests`가 **선언만 남아 있습니다.** 정리 대상.

### 3.3 `bugs.py` — 잘못된 루프 종료 조건 — ✅ **해결**

`if len(results) >= limit: break`로 수정됐습니다. 매 iteration마다 전체 DOM을 다시 select하던 비효율도 함께 사라졌습니다.

### 3.4 `musicbrainz.py` — 가짜 contact 이메일 — ✅ **무효화**

GitHub URL로 교체했다가, 파일 자체가 삭제되었습니다.

### 3.5 외부 API 키 누락 시 침묵 — ⚠️ **미해결**

`services/youtube.py`가 여전히 `log.warning("YOUTUBE_API_KEY not configured")`만 하고 `None`을 반환합니다(35, 68, 102행). 호출자는 "키 누락"인지 "검색 결과 없음"인지 구분할 수 없습니다.

다만 **운영상 더 큰 문제가 따로 있습니다.** `search.list`는 호출당 100 유닛이고 `/parse`가 2회 호출하므로, 기본 쿼터 10,000/일 기준 **하루 약 50회 파싱**이 상한입니다. 키를 넣어도 이 천장이 먼저 옵니다. 두 호출을 하나로 합치면(Topic 판별은 이미 클라이언트 측) 두 배가 됩니다.

---

## 4. Medium

### 4.1 공용 함수 4중 복제 — ✅ **해결**

`core/scoring.py`로 통합됐습니다. 다만 통합에 그치지 않고 **함수 자체가 재설계**되었습니다 — 선형 `0.6*제목 + 0.4*아티스트`는 "아티스트가 다르니 커버다"라고 말할 수 없다는 게 드러났기 때문입니다(아티스트에서 잃는 0.4를 제목에서 벌충 가능). 현재는 곱셈 결합 + 게이트 + 변형 표지 벌점 구조입니다.

### 4.2 죽은 파일 / 테스트 스크립트 — ✅ **해결**

`melon_detail.py`, `melon_test.py` 모두 트리에 없습니다.

### 4.3 로깅이 전부 `print` — ✅ **해결**

백엔드 전체에서 `print(` 0건. 전 모듈이 `logging.getLogger(__name__)`을 씁니다.

### 4.4 응답 디버그 로그가 HTML 본문 노출 — ✅ **해결**

`res.text[:300]` 패턴이 남아 있지 않습니다.

### 4.5 Magic threshold가 코드 곳곳에 박힘 — ✅ **해결**

전부 `core/scoring.py` 상단으로 모였습니다. 값도 재조정됐습니다.

| 상수 | 이전 | 현재 |
|---|---|---|
| `SEARCH_MATCH_THRESHOLD` | 0.5 | 0.62 |
| `EXACT_MATCH_THRESHOLD` | 0.85 | 0.90 |
| `WEAK_MATCH_THRESHOLD` | — | 0.55 (신규) |
| `MUSICBRAINZ_MIN_SCORE` | 70 | 삭제 |

> **주의**: 이 값들은 41곡 벤치마크로 **반증되지 않았습니다.** 임계값을 0.30~0.95 어디에 두어도 벤치 점수가 41/41로 동일합니다. 벤치가 포화 상태라 스코어러의 세부 튜닝을 검증하지 못합니다. 값을 바꾸려면 새 데이터가 필요합니다.

### 4.6 `get_song_info_from_platform` 130줄 — ✅ **해결**

`_LOOKUP_BY_PLATFORM` 딕셔너리 분기로 대체됐습니다. 지역 import도 사라졌습니다.

### 4.7 YouTube 곡 정보 파싱 — ⚠️ **미해결**

`services/youtube.py:126`이 여전히 `if " - " in raw_title: artist, song = raw_title.split(" - ", 1)`입니다. 곡명에 ` - `가 들어가면 깨집니다(예: `Aimer - Re:I am - re-recording`).

`ytmusicapi`가 대안으로 거론됐지만, 조사 결과 현재 `filter='songs'`가 ASCII 대조군을 포함해 모든 질의에 0건을 반환합니다. YouTube가 응답 구조를 바꿨고 라이브러리 파서가 밀린 상태로 보입니다. **비공식 라이브러리 위험의 실물 사례**이므로 지금 도입하면 취약점을 더하는 셈입니다.

### 4.8 `Procfile` 운영 배포 시 `uv` 가용성 — ✅ **무효화**

Railway에서 Oracle VM으로 이전하면서 버프팩이 없어졌습니다. `Procfile`은 이제 **무의미할 뿐 아니라 위험합니다** — `--host 0.0.0.0`이라 그대로 실행하면 Caddy를 우회해 uvicorn이 외부에 직접 노출됩니다. 삭제 대상입니다.

---

## 5. Low

- `models/schemas.py`에 `Field(description=...)` 추가 — ⚠️ 미해결
- `__init__.py` 명시적 추가 — ✅ **해결** (전부 존재하고 git에 커밋됨)
- CORS `allow_origins` 환경변수화 — ⚠️ 미해결. `main.py`에 여전히 하드코딩. 다만 실제 필요성은 낮습니다. 이 목록은 *프론트엔드 주소*이고 그건 GitHub Pages에 고정돼 있어서, 백엔드를 옮겨도 바뀌지 않습니다.
- `requirements.txt` 제거 — ✅ **해결** (uv로 완전 이관)
- `tests/` 폴더 부재 — ⚠️ 미해결. 지금은 더 중요해졌습니다. `route_storefronts`, `calc_score`, `qualifiers`, `extract_platform_id` 모두 외부 호출이 없는 순수 함수라 테스트하기 쉽고, 스코어링 로직이 복잡해진 만큼 회귀 위험이 커졌습니다.
- 외부 API retry/backoff — ⚠️ 미해결. iTunes가 분당 약 20회 제한에 429 + `retry-after` 19~29초를 반환하는 게 확인됐으므로 실질적 필요가 생겼습니다.
- sync 라이브러리 캐싱 — ✅ **무효화** (MusicBrainz 삭제)

---

## 6. 신규 발견 (2026-07-27)

### 6.1 벅스 `lookup_by_id`가 모든 입력에서 실패 — 🔴 **미해결**

```
bugs.lookup_by_id("5877580")  → None
bugs.lookup_by_id("30598121") → None
bugs.lookup_by_id("5812157")  → None
```

서버는 HTTP 200으로 정상 페이지(약 107KB, `<title>...벅스</title>` 정상, CAPTCHA 없음)를 반환합니다. 파싱 셀렉터 4개가 전부 안 맞습니다. 벅스가 HTML 구조를 바꾼 것으로 보입니다.

영향이 **비대칭**입니다. `bugs.search`는 정상(sim 1.0)이라 다른 플랫폼에서 벅스를 *찾는* 건 됩니다. **벅스 URL이 입력으로 들어오는 경우만** 404가 납니다.

### 6.2 지원하지 않는 압축을 광고 — ⚠️ 미해결

`melon.py`가 `Accept-Encoding: gzip, deflate, br`을 보내지만 이 환경의 httpx는 brotli를 디코드할 수 없습니다(brotli 패키지 미설치, `uv.lock`에 항목 0건). 현재 멜론이 gzip으로 응답해 문제가 없지만, `br`로 바뀌면 깨집니다. 광고하지 않는 게 맞습니다.

### 6.3 문자체계가 다른 아티스트명은 기권 — ⚠️ 설계상 수용

`지드래곤` vs `G-DRAGON`, `블랙핑크` vs `BLACKPINK`, `에스파` vs `aespa` 모두 `artist_score` 0.0 → 곡 전체 0점. Apple의 KR 스토어프론트가 이런 아티스트를 라틴 문자로 표기하기 때문입니다.

**의도된 동작입니다** — 틀린 링크(-1)가 빈 슬롯(0)보다 나쁘다는 기준입니다. 다만 실제로 놓치는 곡이 있으므로, 로마자 변환 비교를 붙이면 개선 여지가 있습니다.

### 6.4 레이트리밋 방어 부재 — ⚠️ 미해결

자체 구현이 없고 스톡 Caddy에도 속도 제한 모듈이 없습니다(서드파티 모듈 + xcaddy 빌드가 필요). 남용 시 박스가 느려지거나 죽는 쪽으로 실패합니다 — 과금되지는 않으므로 호스팅 선택의 전제와는 일관됩니다.

다만 더 큰 위험은 박스가 아니라 **업스트림**입니다. `/parse` 폭주는 곧 단일 IP에서 멜론·벅스·FLO로의 폭주이고, 이는 IP 차단을 부르는 가장 빠른 길입니다. `/parse`에는 현재 전체 데드라인도, 캐시도, 제한도 없습니다.

---

## 7. 우선순위별 액션 아이템 (갱신)

| 순위 | 작업 | 상태 |
|---|---|---|
| ~~P0~~ | ~~`en_title`/`en_artist` fallback~~ | ✅ 무효화 |
| ~~P0~~ | ~~`musicbrainz.py` 들여쓰기~~ | ✅ 무효화 |
| ~~P0~~ | ~~dead `return None` 정리~~ | ✅ 해결 |
| ~~P1~~ | ~~melon/bugs URL params 위임~~ | ✅ 해결 |
| ~~P1~~ | ~~`match.py` async 전환~~ | ✅ 해결 |
| ~~P1~~ | ~~bugs 루프 종료 조건~~ | ✅ 해결 |
| ~~P2~~ | ~~`core/scoring.py` 통합~~ | ✅ 해결 |
| ~~P2~~ | ~~죽은 파일 정리~~ | ✅ 해결 |
| ~~P2~~ | ~~`print` → `logging`~~ | ✅ 해결 |
| **P1** | **벅스 셀렉터 수정** (6.1) | 🔴 미해결 |
| **P1** | **YouTube 2회 호출 → 1회 통합** (3.5) | 🔴 미해결 |
| P2 | `tests/` 신설 — 순수 함수부터 | 미해결 |
| P2 | iTunes 429 대비 retry/backoff + 캐시 | 미해결 |
| P2 | `Procfile` 삭제, 미사용 의존성 정리 | 미해결 |
| P3 | YouTube 제목 파싱 개선 (4.7) | 미해결 |
| P3 | 아티스트명 로마자 변환 비교 (6.3) | 미해결 |
| P3 | `melon.py` Accept-Encoding 정정 (6.2) | 미해결 |
| P3 | `/parse` 전체 데드라인 + 캐시 (6.4) | 미해결 |

---

## 8. 칭찬할 점

- 라우터·서비스·스키마 분리가 명확하고, 리팩터링을 견뎠습니다.
- 외부 API 실패가 전체 흐름을 막지 않게 격리돼 있습니다.
- `extract_platform_id` 정규식이 깔끔하고 단축 URL 처리가 분리돼 있습니다.
- **"틀린 링크보다 빈 슬롯"** 원칙이 코드 전반에 일관되게 적용돼 있습니다. `itunes.search`가 채택 가능한 후보가 없으면 빈 리스트를 반환하는 것, `calc_score`가 게이트 미달 시 0점을 주는 것 모두 같은 판단입니다. 링크 변환기에서는 옳은 선택입니다.
- `core/scoring.py`의 주석이 **왜 이렇게 생겼는지**를 설명합니다. 특히 "문자열 유사도로는 번역을 검증할 수 없다"는 근본 제약과, 선형 가중합이 왜 커버곡에 매수당하는지를 남겨둔 것은 다음 사람이 같은 함정에 빠지는 걸 막아줍니다.

---

리뷰 완료.
